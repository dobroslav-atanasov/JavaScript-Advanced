const assert = require('chai').assert;
const Circle = require('./soluiton');

describe('test', function () {
    let ab;
    beforeEach(function () {
        ab = new Circle(5, 6);
    });

    it('test', function () {
        
    })
});